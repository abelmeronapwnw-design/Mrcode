-- ================================================================
-- SXE Auto TP + Botón (CORREGIDO - Delta compatible)
-- ================================================================
print("[SXE] Script corregido ejecutándose...")

-- Elimina cualquier script anterior con el mismo nombre (por si está guardado)
pcall(function()
    for _, scr in ipairs(game:GetService("CoreGui"):GetChildren()) do
        if scr.Name == "SXE_TP_BUTTON" then scr:Destroy() end
    end
end)

-- ================================================================
-- CARGA SEGURA (reemplaza la línea 1 problemática)
-- ================================================================
local function waitForGame()
    if type(game.IsLoaded) == "function" then
        while not game:IsLoaded() do task.wait() end
    else
        task.wait(1)
    end
end
waitForGame()

-- Resto del script...
-- (copia todo el script que te di en el mensaje anterior a partir de aquí)
